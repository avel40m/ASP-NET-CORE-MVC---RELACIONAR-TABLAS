@model presonasimagen.Models.TitulosModel

@{
    ViewData["Title"] = "Create";
}

<a asp-action="Index" class="nav-link"><i class="fas fa-arrow-left"></i> Volver</a>

<h4>Crear categoria</h4>

<div class="row">
    <div class="col-md-8">
        <form asp-action="Create" autocomplete="off">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="row">
                    <div class="col-md-6">
                        <input asp-for="Titulo" class="form-control" placeholder="Nombre de categoria" />
                        <span asp-validation-for="Titulo" class="text-danger"></span>
                    </div>
                    <div class="col-md-2">
                        <input type="submit" value="Crear categoría" class="btn btn-success" />
                    </div>
                </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
